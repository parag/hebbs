[advisories]
ignore = [
    # paste: unmaintained, transitive dep via tokenizers -> macro_rules_attribute.
    # No direct fix until HuggingFace tokenizers drops the dependency.
    "RUSTSEC-2024-0436",
]
